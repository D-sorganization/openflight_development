# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/

# Python scratch
__pycache__/
*.pyc
.venv/

# Session data too large for git (keep summaries, not raw dumps)
*.l3dump
session_*.jsonl
*.pkl

# Local-only scratch
scratch/

# The technology review moved to vendor/affinedrift/ (a git submodule); its
# LaTeX build artifacts are AffineDrift's .gitignore concern now, not this
# repo's.
